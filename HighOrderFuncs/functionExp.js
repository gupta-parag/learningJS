// function Statements
function add(x, y) {
  return x + y;
}
//Function Expression

// Functions are objects too

const substract = function (x, y) {
  return x - y;
};

console.log(substract(10, 3));

//Open this in browser to see it as an object
console.dir(substract);
